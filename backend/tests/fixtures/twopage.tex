\documentclass{article}
\begin{document}
This resume is deliberately spread across two pages so the single-page guard
must reject it. Page one holds this paragraph and nothing else of note.
\newpage
Page two exists only to push the page count past one.
\end{document}
